# S2 — Staging vs producción, migraciones, dominio/HTTPS, smoke test

> Prompt listo para pegar en una sesión nueva de Claude Code (rama nueva desde `main`).
> Ejecutar DESPUÉS de que la muestra de referencia esté en producción. Requiere pasos manuales de Ricardo (Supabase + Vercel); el prompt lo deja claro.

---

Context: Café Sensible (Next.js 16.2.4 App Router, Prisma 7 client at app/generated/prisma, Supabase auth+realtime+Postgres, Vercel, next-intl es/en). Read CLAUDE.md first. Production = Vercel project deploying `main` to https://cata-cafe-opal.vercel.app, single Supabase project (runbook names it, ref dpkgcgdywjfmffqnkqsk, eu-central-1). Launch remediation shipped 2026-09-14 (docs/LAUNCH-REPORT-2026-09-14.md, docs/HANDOVER.md). Kim/Ricardo's product meeting (2026-09) requires that from now on changes go develop → test → validate → publish, with separate testing and production databases.

Facts already established by a prior audit (verify quickly, don't re-audit from scratch):
- No staging concept anywhere: zero hits for VERCEL_ENV / APP_ENV; only NODE_ENV binary checks (lib/env.ts:33 throw-vs-warn, lib/prisma.ts, app/actions/dev.ts, app/api/dev/sign-in/route.ts, app/[locale]/dev/page.tsx, components/dev/DevRoleBadge.tsx, components/pwa/ServiceWorkerRegister.tsx:20). Vercel Preview builds also have NODE_ENV=production.
- No CI (.github/ has only copilot-instructions.md). vitest has 9 pure-lib test files; no typecheck npm script.
- No .env.example (.gitignore ignores .env*). Canonical variable list: CLAUDE.md "Environment Variables" + README.md:417-444. 8 required in prod (lib/env.ts REQUIRED_IN_PRODUCTION), DIRECT_URL migrate-only, optional GEMINI_*, ANALYTICS_*, EMAIL_FROM, DB_POOL_MAX.
- prisma.config.ts hard-codes dotenv .env.local then .env; all 6 DB scripts in scripts/ do the same. The local .env.local currently points at the PRODUCTION database.
- vercel.json declares two crons unconditionally; cron routes only check CRON_SECRET (app/api/cron/*/route.ts).
- lib/email.ts sends whenever RESEND_API_KEY is set, and that key is required in prod. lib/guestClaim.ts falls back to the service-role key when GUEST_CLAIM_SECRET is unset.
- app/robots.ts and app/sitemap.ts build from NEXT_PUBLIC_SITE_URL with no noindex gate; next.config.ts sends HSTS max-age=63072000 includeSubDomains unconditionally, no CSP.
- app/api/health returns 204 with no body by design (offline probe) — do not reuse it as an env check.
- Manual SQL a fresh Supabase project needs: prisma/sql/rls_and_triggers.sql PHASE 2…18b (two overlapping numbering series; PHASE 15 is comment-only; PHASE 18 must be followed by 18b; runbook §1 step 7 notes a REVOKE on rls_auto_enable() that errors on a fresh project) plus prisma/sql/phase1_indexes.sql, Realtime publication for evaluations + cupping_sessions (Phase 7), evaluations REPLICA IDENTITY FULL, anonymous sign-ins enabled (PHASE 14 first), auth redirect URLs. Prisma migrations: 25 dirs (two share timestamp 20260721000000 — deterministic by name), migration_lock.toml present, known checksum drift on the prod _prisma_migrations table (runbook §2 warns).
- README.md:447-453 recommends `prisma migrate dev`; runbook §2 uses `migrate deploy`. docs/HANDOVER.md:59 still says catasensible.ai (correct domain is cafesensible.ai, runbook §6 corrected 2026-09-22) and its "Next action" is stale. Runbook §4 log drain and §5 authenticated smoke test are still owed; §6 domain cutover has no DNS/SSL detail.

Decisions already made (do not re-ask): staging = a SECOND Supabase project (free tier, same region) + the SAME Vercel project's Preview environment with env vars scoped to a `staging` branch; local dev points at staging from now on; production stays on `main`.

Deliverables (code, all additive, small):
1. lib/appEnv.ts: getAppEnv(): "production"|"staging"|"development" from NEXT_PUBLIC_APP_ENV ?? (VERCEL_ENV==="preview" → staging) ?? NODE_ENV; pure + tests/appEnv.test.ts.
2. Staging behaviour: amber server-rendered banner "ENTORNO DE PRUEBAS — los datos aquí no son reales" (components/layout/EnvBanner.tsx, mounted in app/[locale]/layout.tsx, i18n es/en); robots disallow + X-Robots-Tag noindex when not production; both cron routes return 200 {skipped:"non-production"} before the bearer check when not production; RESEND_API_KEY optional on staging (emails become logged no-ops) — keep the rest of REQUIRED_IN_PRODUCTION strict; dev sign-in route, app/actions/dev.ts, /[locale]/dev page and DevRoleBadge gate on getAppEnv()!=="production" instead of NODE_ENV==="development" so Ricardo and Kim can test on staging with seeded users.
3. .env.example committed (names + comments, no values) and .gitignore exception.
4. Script/migration safety: scripts/_env.ts loads ENV_FILE ?? .env.local, prints the resolved DB host, refuses production hosts unless CONFIRM_PROD=yes; all DB scripts import it; prisma.config.ts honours ENV_FILE the same way. npm scripts: typecheck, db:migrate:staging, db:migrate:prod (guarded), env:verify.
5. scripts/verify-env.ts (read-only): RLS enabled on every public table, the policy-referenced functions keep EXECUTE for authenticated (is_session_participant etc., see PHASE 18b), supabase_realtime contains evaluations + cupping_sessions, evaluations REPLICA IDENTITY FULL, prints the inspected host.
6. prisma/sql/bootstrap_fresh_project.sql: consolidated, idempotent net effect of PHASES 1–18b + phase1_indexes.sql (final function bodies only, CREATE OR REPLACE, DROP POLICY IF EXISTS). rls_and_triggers.sql stays the historical ledger; document that new PHASES are appended to both.
7. CI: .github/workflows/ci.yml on PR + push to staging/main: npm ci, prisma generate, tsc --noEmit, eslint (decide: fix the 4 baseline errors first or use a non-blocking step — ask Ricardo), vitest run, `prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --exit-code` (needs a shadow DB — if unavailable in CI, use a Postgres service container).
8. Docs: docs/ENVIRONMENTS.md describing three workflows — (a) a change travels dev → staging → prod: feature branch against staging DB, `migrate dev` on staging, PHASE SQL applied to staging, PR → `staging` (CI + Preview URL with banner), smoke checklist on staging, merge staging → main, `db:migrate:prod` (guarded) BEFORE the push, PHASE SQL on prod, deploy; (b) bootstrapping/rebuilding an environment end to end (Supabase project → .env → migrate deploy → bootstrap SQL → Realtime/anon/redirect URLs → import:reference + import:benchmarks → seed:test → env:verify); (c) domain cutover to cafesensible.ai with DNS records (apex A 76.76.21.21, www CNAME cname.vercel-dns.com or Vercel nameservers), Vercel Domains + www→apex redirect, automatic certificate, NEXT_PUBLIC_SITE_URL (Production only), Supabase Site URL + redirect list (/auth/callback and /**), REDEPLOY, verification (HTTPS+HSTS, magic link, freshly printed QR, /es/opengraph-image, robots/sitemap host). Also: rewrite runbook §6 with that detail, fix HANDOVER.md domain typo + stale Next action, README "Environments & deploy" section reconciling migrate dev vs deploy, CLAUDE.md env section.

Manual steps for Ricardo (write them as a click-by-click checklist in docs/ENVIRONMENTS.md, then STOP and hand over — do not attempt them yourself; never print secret values): create the staging Supabase project; repoint .env.local to it; run the bootstrap; set Preview env vars scoped to branch `staging` (+ NEXT_PUBLIC_APP_ENV=staging, distinct GUEST_CLAIM_SECRET and CRON_SECRET); create + push `staging`; configure staging auth URLs + anonymous sign-ins; run runbook §5 on staging (first time ever end to end) and record it in HANDOVER.md; only then the domain cutover.

Verification you must do before handing over: npm test (incl. appEnv), tsc, lint at baseline, CI workflow green on the PR, the guard test (CONFIRM_PROD unset + .env.local at prod → db:migrate:prod refuses and prints the host), and — once Ricardo gives you staging credentials — env:verify passes on staging, the Preview shows the banner, robots disallows, the cron curl returns {skipped}, dev sign-in works on staging and 404s on production.

Orchestration: you (Fable) design appEnv/env/CI/bootstrap SQL and review; delegate to Sonnet: EnvBanner + i18n, .env.example, script header edits, README/ENVIRONMENTS docs. Give every subagent an explicit file list; forbid bare `git stash`; do not fix the lint baseline in passing unless the CI decision requires it.
