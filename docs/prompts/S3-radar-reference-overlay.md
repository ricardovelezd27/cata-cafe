# S3 — Radar: yo vs referencia vs grupo (tres polígonos)

> Prompt listo para pegar en una sesión nueva de Claude Code (rama nueva desde `main`).
> Ejecutar SOLO si la cata de fin de mes valida el concepto de referencia (ver docs/product/experimento-referencia-2026-09.md).

---

Context: Café Sensible (see CLAUDE.md). The session owner can mark one sample as "Referencia" (CuppingSession.referenceSampleId, shipped 2026-09; results show a badge and a signed Δ vs the reference computed by lib/referenceDelta.ts). Kim's methodology now wants the results radar to overlay THREE polygons per sample: my evaluation, the reference sample, and the group mean, so a cupper sees where they match the reference, where the group agrees, and where the master and the group diverge. Standing product law in PRODUCT.md: "el puntaje necesita contexto" — every number ships with its comparison, display-only.

Facts: components/results/SampleRadarChart.tsx renders two Recharts <Radar> series (mine, community) from radarData rows {subject, mine?, community?} built over AFFECTIVE_ATTRIBUTES (lib/constants.ts); community values come from sample.aggregateScore.attrAverages keyed by the Spanish attribute LABEL (a contract shared with the SQL trigger recompute_aggregate_score in prisma/sql/rls_and_triggers.sql PHASE 6). The chart keeps a private copy of SampleResult (lines ~48-59) instead of importing results/types.ts. Colours come from components/results/chartColors.ts (ChartColors {mine, community, grid, axisText}, CSS custom properties with hex fallbacks — the ONE sanctioned hex exception). The reference sample's attrAverages (group) and the viewer's own affective data for the reference sample are already in the same session.samples array passed to ResultsClient — no new query. results.help.grafico (messages/es.json, en.json) currently describes two series. Hardcoded Spanish strings exist in SampleRadarChart ("Revelar", "Puntuación comunidad", "Sin datos afectivos registrados").

Deliverables: (1) a third series "reference" — for group sessions the reference sample's group attrAverages, for solo sessions the viewer's own scores on the reference sample; passed as a prop `referenceProfile?: Record<label, number> | null` from ResultsClient (computed once); the reference sample's own card shows only two series. (2) ChartColors.reference bound to a new CSS role token in app/globals.css + DESIGN.md (no inline hex outside chartColors.ts). (3) Legend + delta table gain the third column ("Δ ref" per attribute); a pure helper in lib/ (e.g. lib/attributeDelta.ts, vitest-tested) computes per-attribute deltas. (4) Import the shared SampleResult type instead of the private copy; move the hardcoded Spanish strings into results.* i18n while you are there. (5) Rewrite results.help.grafico es/en for three series. (6) PRODUCT.md Shipped line + docs/flows.md §5 results structure. Do not touch scoring, the trigger, or AggregateScore. Verify with tsc, lint baseline, npm test, and a browser check of a closed group session with a reference (three polygons, legend, delta table, reference card with two). Delegate i18n + doc edits to Sonnet.
